---
date: "2026-04-20"
project: "dont"
sessions_analyzed: 0
type: reflection
---

Based on the project context available (CLAUDE.md, git status, and file structure — no conversation transcripts or handoff artifacts yet), here is the reflection content:

## Project-Specific AI Context
_Last reflected: 2026-04-20 · 0 sessions analyzed_

### Conventions
- Project is in **research phase** — work is exploratory; add findings with `wai add research`, not code
- Spec is versioned (`dont-spec-v0.2`, `dont-spec-v0.3.2`) — always check which version is current before referencing spec content
- Multiple AI model transcripts exist (`0-dont-chatgpt.md`, `0-dont-claude.md`, `0-dont-gemini-*.md`) — these are primary research artifacts; read them before generating new analysis to avoid redundancy
- Ontology work is tracked separately (`0-dont-onto-*.md`, `0-ont-dont-gemini-*.md`) — keep ontology concerns distinct from core spec concerns

### Common Gotchas
- No commits yet for most files — all working files are untracked; do not assume anything is persisted or reviewed
- `openspec/` directory exists but contents are unknown — check it before proposing spec formats or schema structures
- `llm.txt` likely contains prompt/model guidance for this project — read it before making AI-related design decisions

### Architecture Notes
- This appears to be a **specification research project**, not a software implementation — the primary output is documents, not code
- The `dont-spec` files suggest iterative spec refinement; treat each version as a distinct artifact and note which version a finding references
- Cross-AI-model comparison is an explicit research pattern here — findings from ChatGPT, Claude, and Gemini sessions should be triangulated, not treated as redundant
