# Agent config projections — defines how configs are synced to tool-specific locations
# Strategies: symlink, inline, reference, copy
#
# Project this repo's wai-managed skills into a local .agents directory
# so agent tooling can discover them consistently.

projections:
  - target: .agents
    strategy: symlink
    sources:
      - skills
