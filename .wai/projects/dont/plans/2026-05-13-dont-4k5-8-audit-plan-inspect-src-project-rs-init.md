---
tags: [pipeline-run:tdd-ro5-2026-05-13-dont-4k5-8-project-initialization-error-paths, pipeline-step:plan]
---

dont-4k5.8 audit plan: inspect src/project.rs init/open paths for I/O and serialization failure handling, then drive behavior with integration tests first. Focus init-related failures: (1) re-init still returns structured already-initialised error, (2) malformed pre-existing config at target path must not panic and should return a structured init refusal or already-initialised path consistently, (3) permission-denied/write-failure on init target should return structured error with operation/path context instead of raw OS wording, (4) project init event serialization path should avoid expect in init code. Public interface under test is 'dont init --json', expecting envelope_kind=error, non-empty remediation, and path/operation-specific message on failures; success remains empty envelope with mode payload. Likely affected tests: tests/init.rs existing reinit assertions plus new failure-mode integration coverage for malformed config and filesystem write denial/simulated directory collision. Implementation will follow red->green->refactor and keep each ticket atomic for commit.
