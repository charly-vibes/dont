Decomposed dont-spec-v0_3_2.md §10.2–10.7 into three OpenSpec capabilities: dont-envelope (outer JSON contract, versioning, identity conventions), dont-errors (ErrorResult shape, error codes, remediation invariant, exit codes), dont-cli-surface (universal flags, colour, stdin piping, completions, help). Key design decision: exit codes placed with errors (not CLI surface) because they're the harness decision pivot. Payload types and input schemas deferred to data-model change.
