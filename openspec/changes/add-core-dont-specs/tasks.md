## 1. Decompose the monolithic draft
- [x] 1.1 Define the first OpenSpec capability boundaries for `dont`
- [x] 1.2 Add an initial core capability spec for tool purpose, scope, and invariants
- [x] 1.3 Add a status lifecycle capability spec for entities and transitions
- [x] 1.4 Add a CLI core capability spec for `conclude`, `define`, `trust`, and `dismiss`

## 2. Preserve traceability
- [x] 2.1 Ensure each new capability is clearly derived from `dont-spec-v0_3_2.md`
- [x] 2.2 Keep the proposal scoped to the first split only; defer envelopes, imports, lifecycle-adjacent verbs, and advanced integration sections to later changes

## 3. Validate
- [x] 3.1 Run `openspec validate add-core-dont-specs --strict`
